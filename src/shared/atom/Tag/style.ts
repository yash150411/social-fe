import styled from 'styled-components';

export const Wrapper = styled.div`
  .ant-tag {
    display: flex;
    max-width: max-content;
    flex-direction: row;
    align-items: center;
    padding: 1px 8px;
    gap: 4px;
    border-radius: 8px;
    flex: none;
    order: 0;
    flex-grow: 0;
    border: none;
    background: #ebeff4;
  }

  .tab-content {
    font-style: normal;
    font-weight: 400;
    font-size: 12px;
    line-height: 20px;
  }
`;
